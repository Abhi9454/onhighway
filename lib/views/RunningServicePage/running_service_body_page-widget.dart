import 'package:flutter/material.dart';

class RunningServiceBodyPageWidget extends StatelessWidget {
  const RunningServiceBodyPageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
